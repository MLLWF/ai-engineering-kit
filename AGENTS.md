# AI 工程入口

本仓库的 AI 协作要求：

- 生成 Git 提交记录时，优先使用中文。
- 提交标题保持简洁、可直接使用，说明本次变更的实际结果。
- 推荐格式：`<类型>：<简短变更结果>`，例如 `测试：完善冲突处理流程覆盖`。
- 常用类型包括：`修复`、`新增`、`测试`、`重构`、`文档`、`构建`、`发布`。
- 正文只在需要交代关键影响、验证或风险时添加，使用短列表，不重复 diff。

